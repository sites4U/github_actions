name: Third Workflow
on: push

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: List files and Copy Repo
        run: ls -a
      - uses: actions/checkout@v4
      - name: List files
        run: ls -a
