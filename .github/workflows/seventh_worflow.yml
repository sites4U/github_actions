name: Seventh Workflow
on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Event Variables
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
